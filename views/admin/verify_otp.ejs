<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Verify OTP · SKILLER Admin</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  * { box-sizing: border-box; }
  body {
    margin: 0;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #101322;
    font-family: 'Inter', sans-serif;
    color: #e6e8f0;
    padding: 20px;
  }
  .card {
    width: 100%;
    max-width: 380px;
    background: #171a2b;
    border: 1px solid #2a2e40;
    border-radius: 10px;
    padding: 32px 28px;
  }
  .card h1 {
    font-size: 20px;
    text-align: center;
    margin: 0 0 4px;
  }
  .card h1 span { color: #5eead4; }
  .card p {
    text-align: center;
    color: #767c94;
    font-size: 13px;
    margin: 0 0 24px;
  }
  label {
    display: block;
    font-size: 13px;
    color: #a0a4b8;
    margin-bottom: 6px;
  }
  .field { margin-bottom: 18px; }
  input[type="text"] {
    width: 100%;
    background: #0f1120;
    border: 1px solid #2a2e40;
    border-radius: 6px;
    padding: 10px 12px;
    color: #e6e8f0;
    font-size: 16px;
    letter-spacing: 4px;
    text-align: center;
    outline: none;
  }
  input:focus { border-color: #5eead4; }
  button {
    width: 100%;
    background: #5eead4;
    color: #0a1f1c;
    border: none;
    border-radius: 6px;
    padding: 11px;
    font-weight: 600;
    font-size: 14px;
    cursor: pointer;
  }
  button:hover { filter: brightness(1.08); }
  .error {
    background: rgba(248,113,113,0.1);
    border-left: 3px solid #f87171;
    color: #fca5a5;
    font-size: 12.5px;
    padding: 8px 10px;
    border-radius: 4px;
    margin-bottom: 16px;
  }
  .info {
    background: rgba(94,234,212,0.08);
    border-left: 3px solid #5eead4;
    color: #cdeee7;
    font-size: 12.5px;
    padding: 8px 10px;
    border-radius: 4px;
    margin-bottom: 16px;
  }
  .back-link {
    display: block;
    text-align: center;
    margin-top: 20px;
    color: #767c94;
    text-decoration: none;
    font-size: 13px;
  }
  .back-link:hover { color: #5eead4; }
</style>
</head>
<body>

  <div class="card">
    <h1>Verify <span>OTP</span></h1>
    <p>Enter the 6-digit OTP sent to your email</p>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <div class="info">OTP has been sent to your email. Valid for 5 minutes.</div>

    <form action="/admin/verify_otp" method="POST">
      <div class="field">
        <label for="otp">Enter OTP</label>
        <input type="text" name="otp" id="otp" placeholder="123456" maxlength="6" inputmode="numeric" required />
      </div>

      <button type="submit">Verify OTP</button>
    </form>

    <a href="/admin/forget" class="back-link">&larr; Back</a>
  </div>

</body>
</html>