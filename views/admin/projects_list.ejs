<%- include('header') %>

<div class="page-content">
    <div class="container-fluid px-4">

        <!-- Page Header -->
        <div class="page-header-bar">
            <h2>PROJECTS</h2>
            <a href="/admin/projects" class="header-action-btn" title="Add New">+</a>
        </div>

        <div class="row">
            <div class="col-12">

                <div class="table-responsive">
                    <table class="table table-bordered table-hover align-middle mb-0">

                        <thead class="table-light">
                            <tr>
                                <th width="80">#</th>
                                <th>Project Name</th>
                                <th>Project Type</th>
                                <th>Image</th>
                                <th width="160" class="text-center">Action</th>
                            </tr>
                        </thead>

                        <tbody>
                            <% if(project.length > 0){ %>

                                <% for(let i = 0; i < project.length; i++){ %>
                                    <tr>
                                        <td><%= i + 1 %></td>

                                        <td>
                                            <strong><%= project[i].pname %></strong>
                                        </td>

                                        <td>
                                            <%= project[i].ptype %>
                                        </td>

                                        <td>
                                            <img src="/<%= project[i].pimg %>" width="80" height="60" class="img-thumbnail">
                                        </td>

                                        <td class="text-center">

                                            <a href="/admin/edit_projects/<%= project[i].pid %>"
                                                class="btn btn-warning btn-sm me-1"
                                                title="Edit">
                                                <i class="fas fa-pen"></i>
                                            </a>

                                            <a href="/admin/delete_projects/<%= project[i].pid %>"
                                                class="btn btn-danger btn-sm"
                                                title="Delete"
                                                onclick="return confirm('Are you sure you want to delete this project?')">
                                                <i class="fas fa-trash"></i>
                                            </a>

                                        </td>
                                    </tr>
                                <% } %>

                            <% } else { %>

                                <tr>
                                    <td colspan="5" class="text-center py-4">
                                        No projects found.
                                    </td>
                                </tr>

                            <% } %>
                        </tbody>

                    </table>
                </div>

            </div>
        </div>

    </div>
</div>

<%- include('footer') %>

